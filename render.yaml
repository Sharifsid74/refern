services:
  - type: web
    name: telegram-bot
    env: docker
    plan: free
    dockerfilePath: Dockerfile
    envVars:
      - key: BOT_TOKEN
        value: your_bot_token_here
    healthCheckPath: /